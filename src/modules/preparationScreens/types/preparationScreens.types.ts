// Tipos específicos del módulo PreparationScreens
// Los tipos inferidos de Zod (PreparationScreen, CreatePreparationScreenDto, etc.)
// se definen y exportan desde los archivos correspondientes en ../schema/

export {}; // Asegura que el archivo sea tratado como un módulo